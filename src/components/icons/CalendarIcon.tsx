const CalendarIcon: React.FC<{ className?: string }> = ({ className }) => {
  return (
    <svg className={className} /* ... svg attributes ... */>
      {/* SVG content */}
    </svg>
  );
};

export default CalendarIcon;